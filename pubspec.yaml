name: shuttle_run
description: shuttle_run

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  audioplayers: ^0.15.1
  path_provider: ^1.5.1
  cupertino_icons: ^0.1.3
  intl: ^0.16.1
  url_launcher: ^5.5.0

dev_dependencies:
  flutter_test:
    sdk: flutter


flutter:
  uses-material-design: true
  assets:
    - assets/sounds/
